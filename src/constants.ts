export let gridSize = 0;  // will hold size of grid
export function setGridSize(_n: number) {
  gridSize = _n;
}
export let cellSize = 0;  // will hold grid cell
export function setCellSize(_n: number) {
  cellSize = _n;
}
export let microCellSize = 0;  // will hold grid micro-cell
export function setMicroCellSize(_n: number) {
  microCellSize = _n;
}
export let nanoCellSize = 0;  // will grid nano-cell
export function setNanoCellSize(_n: number) {
  nanoCellSize = _n;
}

export let seed = 0;
export function setSeed(_n: number) {
  seed = _n;
}

export const HUE_MAX = 360;
export const SATURATION_MAX = 100; 
export const BRIGHTNESS_MAX = 100; 
export const OPACITY_MAX = 100;

